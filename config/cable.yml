# Action Cable uses Redis by default to administer connections, channels, and sending/receiving messages over the WebSocket.
production:
 #adapter: async
 adapter: redis
 url: redis://meetups-env.2swjp7ep9m.ap-southeast-2.elasticbeanstalk.com:6379/1

development:
  adapter: async

test:
  adapter: async
